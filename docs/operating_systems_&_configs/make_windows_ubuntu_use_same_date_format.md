# Make Windows & Ubuntu Use the Same Date Format

To tell your Ubuntu system that the hardware clock is set to 'local' time, open a terminal and execute the following command:

```bash
timedatectl set-local-rtc 1
```
